# dynamicwam.github.io

Project page for DynamicWAM. Hand-written static site, no build step — edit `index.html` and push to `main` to deploy via GitHub Pages.

Media pending: rollout videos go in `assets/videos/`, paper figures in `assets/figures/`.
